import { DayOfWeekPipePipe } from './day-of-week-pipe.pipe';

describe('DayOfWeekPipePipe', () => {
  it('create an instance', () => {
    const pipe = new DayOfWeekPipePipe();
    expect(pipe).toBeTruthy();
  });
});
